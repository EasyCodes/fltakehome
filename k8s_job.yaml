apiVersion: batch/v1
kind: Job
metadata:
  name: map_matcher_job
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: app
          image: your-docker-compose-app-image:latest
          env:
            - name: POSTGRES_URL
            - name: PULSAR_BROKER
